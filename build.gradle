plugins {
    id 'org.jetbrains.kotlin.jvm'
}

subprojects {
    group = 'org.digilinq.platform.users'
    version = '0.0.1-SNAPSHOT'

    apply plugin: 'org.jetbrains.kotlin.jvm'

    repositories {
        mavenCentral()
    }
}

